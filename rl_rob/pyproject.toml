[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rl_rob"
version = "0.1.0"
description = "RL and robotics library"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
include = ["env*", "rl*"]